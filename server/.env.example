# Environment Variables
NODE_ENV=development
PORT=3001

# Database
# Option 1: Supabase (Recommended - Free tier, no installation)
# Get from: Supabase Dashboard → Settings → Database → Connection String (URI)
DATABASE_URL="postgresql://postgres.wvhjnelrebzzfzwzddzl:oYvvuBfeKhWy5RCd@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres"
# For migrations, use Direct Connection URL (without pgbouncer):
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# Option 2: Neon (Serverless PostgreSQL)
# DATABASE_URL="postgresql://[user]:[password]@[endpoint].neon.tech/[dbname]?sslmode=require"

# Option 3: Railway (Simple PostgreSQL hosting)
# DATABASE_URL="postgresql://postgres:[password]@[host].railway.app:[port]/railway"

# Option 4: Local PostgreSQL (requires installation)
#DATABASE_URL="postgresql://postgres:password@localhost:5432/aura_db?schema=public"

# JWT Secret (change in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Optional: AI Service API Keys (for future integration)
# OPENAI_API_KEY=your-openai-key
# ANTHROPIC_API_KEY=your-anthropic-key
